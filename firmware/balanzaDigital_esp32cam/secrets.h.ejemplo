#pragma once

// ─────────────────────────────────────────────────────────────
//  RELLENA LAS DOS LÍNEAS DE WIFI. El resto ya está puesto.
// ─────────────────────────────────────────────────────────────

#define WIFI_SSID ""
#define WIFI_PASSWORD ""

// ─────────────────────────────────────────────────────────────
//  Ya configurado para la sesión de pruebas de hoy.
// ─────────────────────────────────────────────────────────────

// Identifica esta báscula en la web, en MQTT y en los ficheros del FTP.
// Cuando montes la segunda placa, cámbialo (bascula-02, etc.).
#define BASCULA_ID ""
#define BASCULA_NOMBRE ""

// IP de este ordenador, donde corren el broker MQTT y el backend.
// El ESP32 tiene que estar en la MISMA red WiFi (192.168.16.x).
#define MQTT_HOST ""
#define MQTT_PORT         1883

// El Mosquitto de esta máquina acepta conexiones anónimas, así que hoy estos
// dos valores se ignoran. Se dejan por si más adelante activas autenticación.
#define MQTT_USER ""
#define MQTT_PASSWORD ""

#define MQTT_TOPIC_PREFIX ""
